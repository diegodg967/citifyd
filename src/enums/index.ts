export enum TAB_TYPE {
  SEARCH = "search",
  FAVORITES = "favorites",
}

export enum LOCAL_STORAGE_KEYS {
  FAVORITES = "favorites",
}

export enum PLACE_THEME {
  LIGHT = "light",
  DARK = "dark",
}
